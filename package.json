{
  "name": "slowapp",
  "version": "1.0.0",
  "description": "Slow responses to an endpoint",
  "main": "slow-app.js",
  "scripts": {
    "start": "node slow-app.js",
    "start:dev": "nodemon slow-app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "dependencies": {
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.4"
  }
}
